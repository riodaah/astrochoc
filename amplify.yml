version: 1
frontend:
  phases:
    preBuild:
      commands:
        # Instalar dependencias
        - npm ci
    build:
      commands:
        # Ejecutar build
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*

